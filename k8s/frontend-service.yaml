apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: companies-scraper
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: LoadBalancer
